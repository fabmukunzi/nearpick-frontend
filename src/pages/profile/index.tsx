import ProfileComponent from '@components/profile';
import Head from 'next/head';

const ProfilePage = () => (
  <>
    <Head>
      <title>Izimart | Profile</title>
    </Head>
    <ProfileComponent />
  </>
);

export default ProfilePage;
